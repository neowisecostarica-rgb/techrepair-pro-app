{
  "name": "Branch",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string",
      "description": "ID de la organizaci\u00f3n"
    },
    "name": {
      "type": "string",
      "description": "Nombre de la sucursal"
    },
    "address": {
      "type": "string",
      "description": "Direcci\u00f3n f\u00edsica (opcional)"
    },
    "phone": {
      "type": "string",
      "description": "Tel\u00e9fono de contacto (opcional)"
    },
    "active": {
      "type": "boolean",
      "default": true,
      "description": "Estado de la sucursal"
    },
    "sale_lock_token": {
      "type": "string",
      "description": "Mutex persistido para ventas directas de la sucursal.",
      "rls": { "read": false, "write": false }
    },
    "sale_lock_operation_key": {
      "type": "string",
      "rls": { "read": false, "write": false }
    },
    "sale_lock_at": {
      "type": "string",
      "format": "date-time",
      "rls": { "read": false, "write": false }
    }
  },
  "required": [
    "organization_id",
    "name"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "organization_id": "{{user.organization_id}}"
        },
        {
          "organization_id": "{{user.impersonating_org_id}}"
        },
        {
          "user_condition": {
            "is_super_admin": true
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "organization_id": "{{user.organization_id}}"
        },
        {
          "organization_id": "{{user.impersonating_org_id}}"
        },
        {
          "user_condition": {
            "is_super_admin": true
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "organization_id": "{{user.organization_id}}"
        },
        {
          "organization_id": "{{user.impersonating_org_id}}"
        },
        {
          "user_condition": {
            "is_super_admin": true
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "organization_id": "{{user.organization_id}}"
        },
        {
          "organization_id": "{{user.impersonating_org_id}}"
        },
        {
          "user_condition": {
            "is_super_admin": true
          }
        }
      ]
    }
  }
}

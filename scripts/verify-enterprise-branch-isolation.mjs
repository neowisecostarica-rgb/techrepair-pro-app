import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8');const auth=r('base44/functions/operationalGateway/_shared/operationalAuthorization.ts'),gw=r('base44/functions/operationalGateway/entry.ts'),on=r('base44/functions/manageEnterpriseOnboarding/entry.ts'),off=r('base44/functions/manageEnterpriseOffboarding/entry.ts'),ons=r('base44/entities/OnboardingAccessItem.jsonc'),offs=r('base44/entities/OffboardingAccessItem.jsonc');const entities=['EnterpriseOnboarding','OnboardingAccessItem','EnterpriseOffboarding','OffboardingAccessItem'];const checks=[['enterprise read policies are branch-scoped',entities.every(e=>new RegExp(e+": \\{[^\n]+scope: 'enterprise_branch'").test(auth))],['gateway resolves enterprise branch scope',gw.includes("scope === 'enterprise_branch'")&&gw.includes("['branch','enterprise_branch']")],['legacy child rows resolve through parent case',gw.includes('record.onboarding_id || record.offboarding_id')&&gw.includes('entities.EnterpriseOnboarding')&&gw.includes('entities.EnterpriseOffboarding')],['new onboarding access items persist branch',/OnboardingAccessItem\.create\(\{organization_id:auth\.organizationId,branch_id:rec\.branch_id\|\|null/.test(on)],['new offboarding access items persist branch',/OffboardingAccessItem\.create\(\{organization_id:auth\.organizationId,branch_id:rec\.branch_id\|\|null/.test(off)],['child schemas carry branch id',ons.includes('\"branch_id\"')&&offs.includes('\"branch_id\"')],['mutation handlers still deny cross branch',on.includes('Caso fuera de la sucursal autorizada')&&off.includes('Caso fuera de la sucursal autorizada')]];let f=0;for(const[n,o]of checks){console.log(`${o?'PASS':'FAIL'} ${n}`);if(!o)f++;}if(f)process.exit(1);console.log(`\n${checks.length}/${checks.length} Enterprise branch-isolation checks PASS`);
import fs from 'node:fs';const r=p=>fs.readFileSync(p,'utf8');const p=r('src/pages/EnterpriseCommand.jsx'),m=r('src/config/menuConfig.js');const c=[['dashboard uses canonical custody',/AssetAssignment/.test(p)&&/ACTIVE/.test(p)],['dashboard uses canonical offboarding',/EnterpriseOffboarding/.test(p)&&/OffboardingAccessItem/.test(p)],['overdue returns are actionable',/Devoluciones vencidas/.test(p)&&/expected_return_at/.test(p)],['open offboarding and pending access visible',/Offboardings abiertos/.test(p)&&/Accesos pendientes/.test(p)],['evidence is explicitly bounded',/No sustituye evidencia de sistemas externos/.test(p)],['navigation links custody and offboarding',/Custodia de activos/.test(p)&&/Offboarding tecnológico/.test(p)],['enterprise command is in authorized menu',/Control Enterprise/.test(m)&&/ORG_ADMINISTRATION/.test(m)]];let f=0;for(const[n,o]of c){console.log(`${o?'PASS':'FAIL'} ${n}`);if(!o)f++;}if(f)process.exit(1);console.log(`\n${c.length}/${c.length} Enterprise Command & Evidence checks PASS`);
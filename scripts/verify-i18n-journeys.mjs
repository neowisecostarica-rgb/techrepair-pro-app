import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8'),i=r('src/i18n/index.jsx'),q=r('src/pages/VentasCotizaciones.jsx'),crm=r('src/pages/CRM.jsx'),on=r('src/pages/EnterpriseOnboarding.jsx'),off=r('src/pages/EnterpriseOffboarding.jsx');const c=[['all locales include quote CRM and enterprise vocabulary',(i.match(/quotes:\{/g)||[]).length>=5&&(i.match(/crm:\{/g)||[]).length===5&&(i.match(/enterprise:\{/g)||[]).length===5],['quote canonical states keep IDs',/borrador:/.test(q)&&/aprobada:/.test(q)&&/rechazada:/.test(q)&&/vencida:/.test(q)],['quote visible statuses translate',/quotes\.approved/.test(q)&&/estadoConfig\[cotizacionSeleccionada.estado\]\.i18nKey/.test(q)],['CRM semantic labels use i18n',/crm\.prospect/.test(crm)&&/crm\.customer/.test(crm)],['onboarding access statuses translate',/enterprise\.pending/.test(on)&&/enterprise\.confirmed/.test(on)&&/enterprise\.notApplicable/.test(on)],['offboarding access statuses translate',/enterprise\.pending/.test(off)&&/enterprise\.confirmed/.test(off)&&/enterprise\.notApplicable/.test(off)],['enterprise action labels translate',/enterprise\.openRecord/.test(on)&&/enterprise\.confirm/.test(off)]];let f=0;for(const[n,o]of c){console.log(`${o?'PASS':'FAIL'} ${n}`);if(!o)f++;}if(f)process.exit(1);console.log(`\n${c.length}/${c.length} journey i18n checks PASS`);
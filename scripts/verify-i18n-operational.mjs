import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8'),i=r('src/i18n/index.jsx'),w=r('src/config/workOrderStatus.js'),o=r('src/pages/OrdenesTrabajo.jsx'),k=r('src/components/kanban/KanbanBoard.jsx');const c=[['canonical work-order IDs unchanged',['EN_COLA_REVISION','ASIGNADA','EN_REVISION','DIAGNOSTICADA','COTIZADA','APROBADA','EN_REPARACION','PRUEBAS','FINALIZADA','ENTREGADA','CANCELADA'].every(x=>w.includes(x))],['work-order labels have translation keys',(w.match(/i18nKey:/g)||[]).length>=18],['all locales contain work vocabulary',(i.match(/work:\{/g)||[]).length===5],['work-order filters and badges translate at render time',/t\(value\.i18nKey/.test(o)&&/t\(config\.i18nKey/.test(o)],['edit status display translates without mutating stored state',/t\(estadoConfig\[editingOT/.test(o)],['kanban headings translate at render time',/useI18n/.test(k)&&/i18nKey/.test(k)],['authorization and workflow state remain independent of locale',!/locale.*estado|locale.*status/i.test(w)]];let f=0;for(const[n,o]of c){console.log(`${o?'PASS':'FAIL'} ${n}`);if(!o)f++;}if(f)process.exit(1);console.log(`\n${c.length}/${c.length} operational i18n checks PASS`);